# golangci-lint v2 configuration.
# Run locally with `make lint` (requires golangci-lint >= v2).
version: "2"

run:
  timeout: 5m

linters:
  # "standard" = errcheck, govet, ineffassign, staticcheck, unused.
  default: standard
  enable:
    - misspell

formatters:
  enable:
    - gofmt
